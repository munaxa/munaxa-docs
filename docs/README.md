# Munaxa Docs — documentation index

**Purpose:** the entry point to every Munaxa Docs document.
**Audience:** everyone building or reviewing Munaxa Docs, human or AI.

This product's documents live here. Repository-wide rules live in
[`PLATFORM_ENGINEERING_STANDARDS.md`](https://github.com/tam2om/munaxa/blob/main/PLATFORM_ENGINEERING_STANDARDS.md); it lives in the
corporate repository and governs all five. This file is Munaxa Docs' own documentation index.
Where they disagree, the rulebook governs.

```text
docs/
├── README.md            THIS INDEX
├── architecture/        the binding Phase 0 blueprint (00–21) + adr/
├── operations/          the runbooks (Phase 18) — living documents, unlike reports
├── ui/                  how the web client is verified (Phase 5.2)
└── reports/             point-in-time findings — evidence, not guidance
```

**`architecture/` says what a thing *is*; `operations/` says what somebody *does*.** The split
arrived in Phase 18, and it matters in both directions: an architecture document that accumulated
commands becomes a runbook nobody trusts, and a runbook that argues about topology is one nobody
finishes reading at three in the morning. `operations/` is also the one directory here that is
**edited** rather than superseded — a procedure that is out of date is worse than no procedure.

The code the architecture describes exists through Phase 15 — the platform foundation, the whole of
Administration, the per-tenant infrastructure the remaining phases are built on, the document library
that is the first thing to hold a customer's own content, the approval engine that moves a document
through it, the numbering engine that gives an approved document its permanent identifier, the
revision control that publishes what was approved and produces the next controlled version, the
preview pipeline that shows a controlled document without handing its bytes over, and the search
that finds a document by its number, its words and its people without ever disclosing one the
caller may not see, the read half of the audit trail — the timeline, the audit search, the
daily verification with its signed checkpoints, and the evidence bundle — and the deletion half:
one answer to what deleting a document reaches, the recycle bin, the retention schedule, the legal
hold that refuses regardless of permission, and the purge that destroys a record while its trail
survives it, and the delegation that lets one person decide another's approvals without the task
ever moving — a routing overlay whose trail names both people and the arrangement that authorised
them, and the notifications that finally tell people any of it happened: an inbox, an email, a
digest, quiet hours, a suppression when an address stops accepting mail, and one summary where a
sweep would otherwise have sent five hundred, the dashboard that composes all of it without owning
a row of it, and the permission model itself made real at last — ACL entries on the scope tree, the
walk over the materialised paths, deny winning at any level, a folder that can stop inheriting while
the administrators accountable for it still reach through, a list and its total that omit what the
caller may not see rather than hiding it, a screen that says which node decided and why, and a second
factor on the way in, and the reports that finally ask about the whole rather than about one record —
ten of them, each gated on the permission that already governs the surface it summarises and each
scoped by the caller's own reach, exported to CSV, to a real Excel format and to PDF through a
queued job that runs under the requester's reach rather than the consumer's absent one. Its map is
[`apps/api/src/modules/README.md`](../apps/api/src/modules/README.md), and each module carries
its own contract — what it owns, what it depends on, which core port it binds.

## 1. Architecture — the binding blueprint

Every later phase conforms to these or supersedes them with an ADR. Index:
[`architecture/README.md`](./architecture/README.md).

| # | Document | Scope |
| --- | --- | --- |
| 00 | [System Architecture](./architecture/00-system-architecture.md) | C4 context and containers; the system at a glance |
| 01 | [Monorepo & Folder Structure](./architecture/01-monorepo-and-folder-structure.md) | Where every file goes, and why |
| 02 | [Backend Architecture](./architecture/02-backend-architecture.md) | Clean Architecture layers, modules, ports, DI |
| 03 | [Domain Model](./architecture/03-domain-model.md) | Bounded contexts, aggregates, ubiquitous language |
| 04 | [Domain Relationships & ERD](./architecture/04-domain-relationships-and-erd.md) | Every relationship, with cardinality |
| 05 | [Database Design](./architecture/05-database-design.md) | Tables, keys, indexes, constraints, tenancy, soft delete |
| 06 | [Document Lifecycle](./architecture/06-document-lifecycle.md) | States, legal transitions, illegal transitions |
| 07 | [Workflow Architecture](./architecture/07-workflow-architecture.md) | The configurable approval engine |
| 08 | [Permission Model](./architecture/08-permission-model.md) | RBAC, inheritance, overrides, the permission matrix |
| 09 | [Numbering Architecture](./architecture/09-numbering-architecture.md) | Configurable, gapless, never-reused document numbers |
| 10 | [Revision Architecture](./architecture/10-revision-architecture.md) | Identity vs revision vs file; check-out/in, compare, restore |
| 11 | [Storage Architecture](./architecture/11-storage-architecture.md) | Provider-agnostic blob storage, dedupe, lifecycle |
| 12 | [Search Architecture](./architecture/12-search-architecture.md) | Metadata + full text + OCR, permission-filtered |
| 13 | [Audit Architecture](./architecture/13-audit-architecture.md) | Append-only, hash-chained, complete |
| 14 | [Preview Architecture](./architecture/14-preview-architecture.md) | Independent renderer plugins, derivative artefacts |
| 15 | [API Architecture](./architecture/15-api-architecture.md) | REST conventions, versioning, errors, pagination |
| 16 | [Frontend Architecture](./architecture/16-frontend-architecture.md) | App Router structure, feature modules, state, caching |
| 17 | [Security Architecture](./architecture/17-security-architecture.md) | AuthN/Z, uploads, encryption, OWASP posture |
| 18 | [Notification Architecture](./architecture/18-notification-architecture.md) | Email, in-app, digests, future push |
| 19 | [Performance & Scalability](./architecture/19-performance-and-scalability.md) | Millions of documents, thousands of users |
| 20 | [Deployment Architecture](./architecture/20-deployment-architecture.md) | Environments, topology, CI/CD, backup and DR |
| 21 | [SaaS Commercial Architecture](./architecture/21-saas-commercial-architecture.md) | Plans, entitlements, metering, provisioning, operator console |

### Decision records

Immutable. Supersede, never edit. [`architecture/adr/`](./architecture/adr/).

| ADR | Decision |
| --- | --- |
| [0001](./architecture/adr/0001-product-root-placement.md) | The product root is `edms/`, not `docs/` |
| [0002](./architecture/adr/0002-multi-tenant-isolation-model.md) | ~~Shared database, row-level tenant isolation, RLS backstop~~ — superseded by 0015 |
| [0003](./architecture/adr/0003-document-identity-revision-file-separation.md) | Document, revision and file are three separate things |
| [0004](./architecture/adr/0004-numbering-assigned-at-approval.md) | Numbers are reserved at submission, assigned at approval, never reused |
| [0005](./architecture/adr/0005-hierarchical-acl-with-deny-precedence.md) | Inherited ACLs on the scope tree, explicit deny wins |
| [0006](./architecture/adr/0006-declarative-workflow-engine.md) | Workflow definitions are versioned data, not code |
| [0007](./architecture/adr/0007-storage-port-and-content-addressing.md) | One storage port; blobs are content-addressed and deduplicated |
| [0008](./architecture/adr/0008-postgres-first-search.md) | PostgreSQL full-text first, behind a search port |
| [0009](./architecture/adr/0009-append-only-hash-chained-audit.md) | Audit is append-only and hash-chained |
| [0010](./architecture/adr/0010-soft-delete-and-retention.md) | Soft delete everywhere, purge only by retention policy |
| [0011](./architecture/adr/0011-transactional-outbox-for-async-work.md) | Async work is dispatched through a transactional outbox |
| [0012](./architecture/adr/0012-entitlements-as-data-enforced-centrally.md) | Plans and entitlements are data, enforced centrally, separate from permissions |
| [0013](./architecture/adr/0013-operator-console-as-separate-surface.md) | Cross-tenant operations live in a separate, fully audited console |
| [0014](./architecture/adr/0014-materialised-path-as-text.md) | The scope tree's ancestry is a materialised path stored as `text` |
| [0015](./architecture/adr/0015-database-per-tenant.md) | One database, storage location and search index per tenant; where each lives is a placement resolved through a registry |
| [0016](./architecture/adr/0016-inheritance-break-truncates-the-chain.md) | An inheritance break truncates the ACL chain, for both effects; the list predicate is regions |
| [0017](./architecture/adr/0017-electronic-signature-as-witnessed-attestation.md) | A signature is a witnessed 21 CFR Part 11 attestation over a revision's content digest, never a qualified signature |
| [0018](./architecture/adr/0018-machine-identity-as-a-delegated-subject.md) | A machine caller is bound to a person and acts as them, narrowed by scopes — never a principal of its own |
| [0019](./architecture/adr/0019-webhooks-are-not-notifications.md) | A webhook is its own delivery path; `NotificationChannel.WEBHOOK` is a value nothing uses |
| [0020](./architecture/adr/0020-key-management-and-rotation.md) | The deployment's secret store *is* the key management service; what the product owes is one key per purpose and a rotation each sealed value can survive |

## 2. Reports

Point-in-time evidence. **Historical, never edited afterwards** — superseded, not revised.

### Phase 6.3 — authorization and permission enforcement completeness

> The third phase from the Phase 6.0 roadmap, and the one that had to disprove a finding of its own.

| Document | Purpose |
| --- | --- |
| [Phase 6.3 — Authorization & Permission Enforcement Completeness](./reports/phase-6.3-authorization-enforcement.md) | The P1 Phase 6.2 raised about `context.roles` carrying role keys where the ACL subject expects identifiers — investigated empirically and **disproved**, along with the claim that the integration suite had caught it. Neither was true: the failure that prompted the change reported a *domain* refusal rather than a reach refusal, its real cause was a metadata field the test had invented, and `roleIdsFor` matches `role.key` or `role.id` by design at a single deliberate boundary. Four tests now prove a role key and a role id produce identical decisions down to the node that decided them — and that an unknown name and another tenant's role id both grant nothing, because a tolerance that accepted anything would be a hole. Why `library:view` was not the phantom the audit called it but a real gap wearing a phantom's clothes: the capability existed and was gated on `library:manage`, so an auditor — seeded with view and deliberately without manage — could not list a library at all, and the document browser's library selector showed them nothing. Why `report:manage` was left unused, again. And the blind spot in a boot-time assertion that had run for eighteen phases: it checked every route against the catalogue and never the catalogue against the routes, which is exactly where two permissions hid. The third check added here found a fourth on its first run — and that one turned out to be enforced in a handler body, because whether a decision is a rejection is a property of the request rather than of the route |
| [Phase 6.4 — Event Consumers & Notification Completeness](./reports/phase-6.4-event-consumers-notifications.md) | Sixty-six declared event types traced to their actual publish call sites, and the discovery that a route is not a producer: `document.approved` and `document.rejected` had a catalogue entry, `en` and `ar` templates, a consumer branch, a lane and a test asserting they reach it — and no code had ever published one, so two of 18 §4's named notifications had never been sent to anybody. Approval and rejection run through `DocumentService.transition`, which wrote an audit row and no outbox row. Why nothing caught it: the notification suite hands the consumer a synthetic event, which proves translation rather than production. The second gap was the one row of 18 §7's reliability table nothing had built — a transient send wrote `FAILED`, `claimQueued` selects `QUEUED`, and `DeliveryState.FAILED` was read by no query in the product, so a provider unreachable for a minute lost every email queued in it, permanently and invisibly because the in-app copy survived. Retry now uses the columns that already existed. And an authorization defect found while tracing recipient resolution: a controller documenting that none of its routes takes a user identifier had one that did, so any signed-in colleague could clear somebody else's unread marker — both guarding tests confirmed to fail against the old predicate. Ten event types stay unproduced, three lifecycle events stay deliberately silent, and three limitations are named rather than worked around, because the objective was a truthful system rather than a maximal one |
| [Phase 6.5 — Enterprise API Surface & Missing UI Completion](./reports/phase-6.5-api-surface-and-ui-completion.md) | Thirty-four controllers traced route → controller → service → authorization → **actual caller**, and the finding that five complete backends had no caller anywhere in the product. Document templates were the clearest: a twenty-column model, five routes, a 373-line service, soft delete and restore, `template:manage`, and `grep -rn "document-templates" apps/web` returning nothing at all — an entire domain unreachable. It now has one canonical screen in Configuration, built from the existing admin primitives with no component created and no API file touched. Search rebuild needed a placement decision rather than a screen: it is an operator action that was already asynchronous, so it sits on the settings page that already requires its permission, because building an operations console for one button is what the brief rules out. Why the status word is PARTIALLY COMPLETE rather than COMPLETE — four verified gaps remain open, none blocked, each specified and sized: signatures, whose backend is complete and whose UI is a Part 11 re-authentication ceremony that deserves its own threat model; legal holds, where the dashboard already counts them and links to a screen that administers something else; and the identity-provider and audit-sink configuration screens. Also why `useAdminColumns` was declined for one screen: it is typed on a record shape the template contract does not carry, and widening an API response to satisfy a column helper is changing a shape to suit a screen |
| [Phase 6.8 — Post-Phase-6 Production Verification](./reports/phase-6.8-post-phase6-production-verification.md) | **NO-GO as found**, on one P0 the audit was designed to catch: the document preview viewer issued **6,704 presigned content URLs in 1.5 seconds** from a single mount — each an audited issuance, so an open viewer was both a denial of service against the tenant's own API and a flood through its compliance trail. One dependency array, rooted in the `useTranslate` identity trap this phase was asked to verify: `translatorFor` allocates a new closure per call, so a `useCallback` listing `translate` was new every render, an effect *called* it, the call set state, and the loop closed. Fixed under the blocker clause and guarded by the only observable that could see it — a count. Nothing else was fixed. Every other control was traced declaration → binding → runtime path and **exercised**: the eleventh sign-in from one address is a real `429`; RLS is ENABLED *and* FORCED on every discovered tenant table and an identifier from one tenant finds nothing in another; the audit chain refuses `UPDATE` even for the owner; 278 of 279 route handlers carry a permission and the one exception is `GET /auth/me`; all 39 catalogue permissions are routed, handler-enforced or declared reserved — **no phantom permissions**; session cookies are `HttpOnly`/`Secure`/`SameSite=Lax` with `document.cookie` empty in a real browser. Confirmed as real and left documented: `Retry-After` is promised by three artefacts and sent by none, and field-level `errors[]` die at one line in the web API client. The first phase in this sequence where *"is the control actually reachable?"* answered yes every time — and where what blocks production is not code, but an unrehearsed restore and eight web workflows nobody has ever run |
| [Phase 6.6 — Electronic Signature & Witnessed Attestation UI](./reports/phase-6.6-electronic-signature-ui.md) | **COMPLETE** — the ceremony, and the two production-breaking defects building it uncovered. `Sign` opens a review rather than signing: the statement stage shows the server's exact bytes in a focusable, scrollable region with **no credential field on it at all**, which makes "read before you attest" a property of the markup rather than of the copy. Re-authentication is a stage *of* the confirmation rather than one before it, and the reason is stated rather than glossed — there is no re-authentication endpoint, deliberately, because a standalone "check my password" call is the credential oracle ADR-0017 §6's single undifferentiated refusal exists to prevent. Credentials live nowhere: uncontrolled inputs read out of `FormData` at submit, never in state, and the field is *gone* rather than cleared after success. Proven in real Chromium against a booted API and a booted web server, unmocked: the stored `statement_body` differs from what was displayed in `signed-at` and nothing else; preview and cancellation write neither a signature nor an audit row; a reader is refused the preview route with `403`. **And the part that matters most:** booting the application for the first time in this repository's history found that `/login` answered `500` in every built deployment — a `'use server'` module exported a plain object — and that the document screen and the document library both requested `pageSize: 200` against a maximum of 100, so *neither page could be opened, in any environment, since it was written*. Six phases of green suites, because nothing below the login page had ever loaded the product |
| [Phase 6.6A — Electronic Signature Statement Preview API](./reports/phase-6.6a-signature-statement-preview.md) | **COMPLETE**, and Phase 6.6 is unblocked: one additive `GET /documents/:id/signatures/statement`, behind `document:sign` and the same `@ScopedTo` binding, returning the canonical §11.50 manifestation before there is a signature to verify. The point of the phase is that there is now exactly **one** call to `serialiseSignatureStatement` in the product and both callers reach it — `signWithin` became four ordered steps so the duplicate check still runs where it ran before, and the facts type is `Omit<SignatureStatement, …>` so a field added to what a signature attests is a field the preview must resolve rather than one it could silently omit. Proven the only way worth proving: a preview taken over HTTP, the same revision signed over HTTP, the stored `statement_body` read from the table, and `['signed-at']` asserted as the *only* differing field — with the stored HMAC recomputed, because a preview matching bytes nothing was signed over proves nothing. The instant is the one thing a preview cannot promise, so `preparedAt` names it instead of hiding it. Also proven: three previews leave `document_signature` and `audit_event` at their prior counts, and eight previews do not spend `document.sign`'s five-per-fifteen-minutes budget. **And the defect the phase was not looking for:** §11.200 re-authentication read `user_credential` outside any unit of work, so with the setting on — its default, and the reason the endpoint takes a password — *every signature this product has ever been asked to take answered `500`*. Six phases of green suites missed it because the uniqueness suite asserts the index directly, the Phase 6.7 HTTP test is refused by authorization first, and a service-level spec is unconstructible: `requireTransaction()` lives in `AsyncLocalStorage` only `PrismaUnitOfWork` can populate. The fourth control in four phases found to be declared, configured and unreachable |
| [Phase 6.7 — Security Control Completion: Rate Limiting & Signature Concurrency](./reports/phase-6.7-security-control-completion.md) | **PARTIALLY COMPLETE.** One of Phase 6.6's two blockers is cleared and one is not, so Phase 6.6 stays blocked. The invariant `document_signature` has described since Phase 16 — *one live signature per person, revision and purpose, partial on `withdrawn_at`* — is now a real partial unique index, and the property is proven by ten rounds of a genuine race: two transactions inserting before either commits, exactly one winning each round, the loser failing with SQLSTATE `23505`. Why it is declared in migration SQL and deliberately **not** in `schema.prisma`: Prisma cannot express a partial unique index, so `@@unique` would tell it the constraint is *total* and a later `migrate dev` could repair it into one, breaking withdraw-then-re-sign — the same convention `uq_user_department_primary` already follows, and a mistake made and reverted here. The race maps to the existing `purpose: duplicate` refusal rather than a 500, matched on the index by name so a future unrelated index cannot be misreported. Also a correction to Phase 6.6's evidence: `RATE_LIMIT_RULES` is a six-rule per-surface table that a narrow grep missed — still consumed by nothing, but it makes Part A *wiring* rather than design. Part A was not started and is not dressed up: no limiter, no two-instance test, and the Redis-unavailable fail-safe policy remains an undecided question rather than an assumed one |
| [Phase 6.7B — Distributed Rate Limiting Completion](./reports/phase-6.7b-distributed-rate-limiting.md) | The survey that overturned its own brief. Phase 6.7B was to wire the existing `@munaxa/security` RateLimiter into the request pipeline; that package **does not exist** in this workspace, nor does `@munaxa/cache`, and `@munaxa/platform` is a frontend package whose `exports` map is tokens, themes, icons, hooks, patterns, layouts, shell, date and charts — with no server-side surface at all, and no dependency on it from the API. So there was no Platform limiter to wire and no `RateLimitTarget` whose expressiveness could be judged, which is the exact condition the brief names for STOP-and-document rather than substitute. Why that is nonetheless not a blocker: the product already owns the primitive in its own tree — `CachePort.increment` is an atomic Redis `INCR` + `EXPIRE NX`, bound and in production use — so a guard over it would be the *first* limiter rather than a duplicate of a Platform one. Also the coverage matrix's two findings: `auth.password-reset` is a rule for a route that does not exist, and the signature endpoint has no rule at all, so no number was invented for it. The two decisions left deliberately open for a person: the Redis-failure policy, and the signature rule's window and maximum |
| [Phase 7.7B — Search Visual Composition & Populated Results](./reports/phase-7.7b-search-visual-composition.md) | **BLOCKED on the phase's own precondition, with the cause measured.** The brief marked populated Search results IMPORTANT NEW REQUIREMENT, so that came first — a real query typed into the real field and submitted through the real form against real PostgreSQL, Redis, API, production build and login. The product answered **"Nothing matches that search"**, and per the failure discipline that was diagnosed rather than retried: `select count(*) from search_index_entry` returns **0**, and `grep search_index_entry scripts/e2e-signature-fixture.mjs` returns **nothing**. The E2E fixture inserts document rows directly and never indexes them, and indexing is application work in `postgres-index.adapter.ts` rather than a database trigger — so a fixture document is listable, openable, and appears on the dashboard's recent row (7.6D proved that), while being **structurally unfindable by Search**. That is why the Search baseline has only ever shown the empty state, and it is a property of a fixture that predates Search, not a defect in the screen. A2 and A3 were deliberately **not** implemented: recomposing a results hierarchy that has still never been seen with results in it would be designing blind. Two real routes are recorded — create the document through the product's own creation workflow so the indexer runs (the same principle 7.6D used when it opened a document rather than faking a view), or drive the real reindex the `search_rebuild` table implies. Phase 7.7A is untouched and its suite re-passes **7/7**; the E2E spec was restored byte-for-byte after the investigation. The recommendation is explicitly *one* follow-up phase, not three — the remaining work is ordinary engineering |
| [Phase 7.7A — Search Bar Responsive Composition](./reports/phase-7.7a-search-bar-responsive.md) | **COMPLETE — and it disproves its own predecessor's hypothesis.** Phase 7.7 guessed the search bar's wrap was a specificity fight between `flex-1`, `basis-full` and `sm:basis-auto`, and honestly recorded the cause as unestablished. Measuring computed styles in the running product settled it the other way: `flex-basis` really was `auto` at 1440/1280/1024/768/640, so `sm:basis-auto` had been applying correctly all along. The actual cause is the platform's own `Input`, which carries **`w-full`** — `flex-basis: auto` resolves the flex base size from `width`, so the field's base size *was* the whole form, it filled the flex line alone, and every sibling wrapped beneath it. The measurement shows it to the pixel: field width equalled form width exactly at all five desktop widths (1120/1120, 960/960, 704/704). The fix is **one class**, `sm:basis-auto` → `sm:basis-0` — base size zero, so the sort control and buttons take their natural widths and the field grows into the remainder. No pixel value, no breakpoint, no token, no override of the platform. After: the field and the submit button share a top edge (171/171) at every desktop width, while **390 keeps `basis: 100%` and its own line**, which is the Phase 7.1 protection against "Save search" hanging past the viewport. The regression test asserts `wrapped === (width < 640)`, so it guards both directions at once — and the **test-proof is the discriminating kind**: reverting the class failed exactly the five desktop widths while **390 correctly kept passing**, since a test that failed there too would be asserting the wrong thing. Also recorded as a platform note: `Input`'s `w-full` makes `basis-auto` unusable inside a flex row, a sharp edge any product composing a search bar will hit |
| [Phase 7.7 — Search Visual Modernization](./reports/phase-7.7-search-visual-modernization.md) | **BLOCKED — audit only, no code changed.** The audit is real and so are its findings; the blocker is session budget, and shipping the fixes without the running-app, RTL, dark, axe and E2E evidence this phase mandates is exactly what its own non-negotiable rule forbids. The headline defect is visible in the existing baseline: **the search bar breaks across two lines**, query on one and `Relevance ▾ / Search / Save search` on the next, so the primary action is detached from the field it operates on. The cause is `flex-1 basis-full sm:basis-auto` — three rules competing for one `flex-basis`, since `.flex-1{flex:1}` itself sets `0%`. `sm:basis-auto` **is** in the built stylesheet, so it is not a missing class, and the `basis-full` is deliberate: Phase 7.1 added it to fix a real 390px overflow where "Save search" hung 24px past the viewport. Classified **A for the symptom, F for the cause** — the wrap is visible, the reason needs a computed-style measurement in a browser rather than a source reading, and it must not be papered over with a fixed width or the 390 overflow returns. Second finding: **Search was missed by the Phase 7.2/7.3 section grammar** — it still hand-rolls `<h2 class="text-lg font-medium">` where every other screen uses `Panel`/`Section`. Also noted: the result count is orphaned mid-page; Search Tips and Index Status have no backing data while Saved and Recent searches are real; and the result-type tabs remain refused, re-confirmed from the contract. The current baseline shows only the **empty** state, so the populated result row has never been inspected — the same gap the dashboard had before 7.6D. Recommends splitting into 7.7A (the search bar, measured) and 7.7B (composition + full evidence), noting infrastructure is not a blocker now that 7.6C/E proved the procedure |
| [Phase 7.6E — Dashboard Recent Documents Final Verification](./reports/phase-7.6e-dashboard-recent-documents-final-verification.md) | **COMPLETE — the populated Recent Documents running-app evidence gap is closed.** 7.6D left four gaps and this closes each against the real product. **Dark** is reached by clicking the top bar's own theme button, not by setting a class — the button is what a person uses and what writes the `edms.theme` preference. **Arabic** is set through the application's real `edms_locale` cookie and a normal navigation, no CSS fake and no test-only attribute; at 1280 and 390 the panel and badge mirror correctly and — the failure RTL actually produces — `SOP-E2E-0001` and `2026/8/10` survive as intact LTR runs inside the RTL line. **axe runs against the live page with the row populated**, which is strictly stronger than the jsdom suites where `color-contrast` must be switched off for want of a cascade: **zero critical or serious violations**. And the row is **clicked** rather than merely asserted to have an `href` — 7.6D proved it *was* a link, which is not the same as the link working, so this follows the click to `/documents/{id}` and asserts the destination is the document rather than an error boundary. 19/19 E2E against real PostgreSQL, Redis, API, production build and login. The five new assertions were **proved able to fail**: removing the document-number span turned 19 green into **6 red covering all four new areas**, and the revert is byte-identical by `git diff`. Not claimed: dark was captured at 1440 only, keyboard traversal to the row was not exercised, and whether `2026/8/10` is the *right* civil-calendar presentation for an Arabic reader is still an unreviewed localization judgement |
| [Phase 7.6D — Dashboard Recent Documents Running-App Verification](./reports/phase-7.6d-dashboard-recent-documents-e2e.md) | **The row is finally real.** Two phases improved the recent-document composition and a third proved the dashboard runs, but the seeded tenant had never opened anything, so the panel showed its empty state and the row existed only as a static render. The mechanism was **read rather than guessed**: `DocumentService.open()` calls `activity.recordView()`, and `@Get(':id')` is the only route that reaches it — so a document becomes recently opened by *being opened*, and by nothing else. No row was written to PostgreSQL, no handler called, no event constructed. The flow is a person's: sign in on the real form, navigate to the document, click Home, read the dashboard. The result, from real data through the real stack: **`Batch release procedure` / `Draft` with its pencil / `PDF · Root · SOP-E2E-0001 · 8/10/2026`** — all six Phase 7.6B fields at once, title dominant and the number sitting quietly in the muted line. It also closes a limitation 7.6B recorded against itself: `FormatBadge` was "unexercised, the fixture has no file", and the `PDF` here is rendered from a real MIME type. **14/14 E2E**, six widths on one session with `scrollWidth <= clientWidth` and the document number asserted at each, because the number is the narrowest thing on the row and the first to clip. The test was **proved able to fail** — deleting the number span turned 14 green into 2 red, and the revert is byte-identical by `git diff`. Not reached, and not rounded up: the populated row in Arabic and in dark, and axe on the live page |
| [Phase 7.6C — Dashboard Running-App Verification](./reports/phase-7.6c-dashboard-running-app-verification.md) | **The three-phase blocker is closed**, and it opens with a correction to its own predecessor. Phase 7.6B declared running-app verification BLOCKED because `pg_isready` and `redis-cli ping` failed — true, and the wrong conclusion: the *servers* were not running but the **binaries were installed all along** (PostgreSQL 16.13 under `/usr/lib/postgresql/16`, Redis 7.0.15). "The service is not answering" and "the service cannot exist here" are different claims, and the previous phase reported the second holding evidence only for the first. No infrastructure was created: the cluster was started by hand following the repository's own CI procedure and migrated by its own runner. The dashboard now runs against real PostgreSQL, real Redis, a real NestJS API, a real production web build and a **real login form with a real session** — no mock, no bypass — in real Chromium. **12/12 E2E pass**, covering all six widths on **one sign-in and one page with six `setViewportSize` calls**, because six logins to measure a layout would spend the same rate-limit budget Phase 7.1C spent a phase diagnosing. The overflow assertion is `scrollWidth <= clientWidth`, which is the check a screenshot cannot make without a human, and it held at 1440/1280/1024/768/640/390. Two boot failures were fixed as configuration rather than bypasses (`JWT_ACCESS_SECRET`, and Chromium's resolver wanting a `headless_shell` build this image lacks). The 7.6A key-leak fix is now **proven against the product** rather than a fixture — the Users tile renders "Active 2" from a real database. The account-chip UUID defect, argued from source since 7.5, is **confirmed by screenshot** and classified P2/PRE-EXISTING, unfixed because `/auth/me` carries no name. What is honestly **not** closed: the seeded tenant has no view history, so the recent-document row this sequence spent two phases improving rendered its empty state and remains verified only statically |
| [Phase 7.6B — Dashboard Recent Documents & Responsive Completion](./reports/phase-7.6b-dashboard-recent-documents.md) | **PARTIALLY COMPLETE**, with one item **BLOCKED** on infrastructure rather than effort. The finding is that the recent-document row was **discarding four fields it already held**: `DocumentSummary` carries `status`, `folderName`, `file.mimeType` and `updatedAt`, and the row rendered a title and a number and threw the rest away — so the fix needed no request, no field and no contract change. The row is now two lines: title at `font-medium` with `DocumentStatusBadge` (reused, not re-implemented, and already word-plus-icon rather than colour-alone), then format · folder · number · date as muted secondary text, so the number stops competing with the title. **Zero requests added.** `updatedBy` is a UUID only, so the reference's "by Sarah Ahmed" is refused for the same reason 7.5 found the account chip showing a raw UUID. RTL is verified **with images for the first time**: four new Arabic baselines show the panel titles and badges mirroring correctly and — the specific risk — `QM-0001` and `2026/1/2` holding together as coherent LTR runs inside the RTL line, no reversed digits. A new 390 baseline shows single-column stacking with the status unclipped. Five regression tests assert what a reader sees rather than how it is built. What is **blocked**: the running application, measured not assumed — `pg_isready` gives no response and `redis-cli ping` is refused, and the E2E harness boots the real API. Third phase running-app verification has been asked for and not delivered; the report says it needs a container with PostgreSQL and Redis, or the now-working CI integration job extended |
| [Phase 7.6A — Dashboard Visual Modernization](./reports/phase-7.6a-dashboard-visual-modernization.md) | **PARTIALLY COMPLETE.** The phase's most useful step was rendering the existing baseline and *opening the image*, which surfaced three defects that five prior visual phases had read past. The sharpest is a real product bug: the Users and Workflow tiles were printing **raw translation key paths** — `dashboard.admin.userState.DRAFT`, `approvals.instanceDRAFT` — on screen. The three breakdown tiles are the only place in the product where a key is *computed from data* rather than written as a literal, so `MessageKey` is a cast the compiler cannot check, and `translate` answers a miss by returning the key, which is right for a literal and wrong for a runtime value. A `labelled()` fallback now renders the enum value instead: `SUSPENDED` is not English and does not pretend to be, but it names the thing counted rather than describing how the product failed to name it. The other two were geometry — seven KPI tiles in a four-column grid is 4 + 3 and five Organisation counts is 4 + 1, so the screen opened on two rows that read as unfinished. Fixed by **grouping rather than padding**: three tiles that are a claim on the reader's time, then four that describe what they hold, and `Columns` already includes `5`. The dashboard is **82px shorter with nothing removed**. Deliberately one `Section` and no second heading, because a heading needs a translated string and inventing Arabic for it is what Phase 7.4C spent three phases forbidding. The donut was evaluated and **refused** — the data is real and the platform ships charts, but a five-slice donut trades aligned numbers for a shape, and what the list actually needed was the key-leak fix. The regression test was **proved to fail** before being trusted (2 of 3 fail with the fix reverted), and it passed vitest while `tsc` rejected it for a missing `total` — the suite-is-not-the-gate lesson, hit again. Not done and stated plainly: **no Chromium run**, and 1440/1024/768/640/390 and Arabic RTL all uninspected |
| [Phase 7.6 — Priority Screen Visual Modernization](./reports/phase-7.6-priority-screen-visual-modernization.md) | **PARTIALLY COMPLETE — one of five priority screens changed, and the report says so in a table rather than in prose.** The shipped change is the library's primary action promoted out of `ResourceList`'s toolbar into `PageHeader.actions`: it had been the last of six controls beside the search box, the deleted filter and the column menu, at identical weight to a dropdown, and the slot it moved into has existed since the platform shipped — four screens used it and the library, with the strongest claim on it, was not one. No new label was minted; it reuses `admin.actions.create`, so the reviewed Arabic is byte-identical. The larger deliverable is the reconnaissance: every platform API read from the installed `.d.ts` rather than assumed, and a reference-to-product mapping covering all five screens. It settles the search-tabs question against the reference — `searchHitSchema` is a **document** hit throughout, `bodySource: 'TEXT' | 'OCR'` is a per-hit attribute rather than a result category, and `facets` are dimension buckets, so All/Folders/Metadata/OCR would mean inventing four categories and their counts. `meta.total` **is** real, so the prominent result count remains legitimate work. What the report refuses to claim is as important as what it delivers: the running application was never driven in Chromium, only 1280/tablet/390 were inspected, RTL was not checked for the change, and no new test was added — the four regenerated baselines are the only thing that would catch the button reverting |
| [Phase 7.5 — Visual Modernization Audit](./reports/phase-7.5-visual-modernization-audit.md) | The audit whose headline finding is a negative one: **there was no local design system to remove.** The shell is already `AppShell`/`Sidebar`/`TopBar`/`SidebarNav`, every list runs through platform `DataGrid` via `ResourceList`, and `grep -rn "from 'lucide-react'"` returns **0** across the app and every package — the icon rule was already satisfied. The real gap is installed capability never used: `NavigationItem.badge`, `Tabs`, `HoverCard`, `Popover`, `Avatar`, `Sparkline` and the chart family at zero uses each. Two of the audit's own findings were **corrected by measurement rather than left standing**: rail badges were ranked the highest-value change until `GET /dashboard` was found to cost ~13 database round-trips across 8 modules, which on a layout means paying it on every navigation; and file-type recognition was written up as missing when the library already renders `formatFor(mimeType)` as a `FormatBadge`. Four things the reference screenshots show cannot be built honestly and are refused with their reasons — the storage quota bar (`storageTileSchema` deliberately has **no quota**, so the percentage has no denominator), the KPI deltas (no period-on-period figure exists), the entity/branch switcher (a scoping dimension, not a visual treatment) and the Expiring Soon tile (not exposed) |
| [Phase 7.5 — Visual Modernization](./reports/phase-7.5-visual-modernization.md) | **PARTIALLY COMPLETE, and says so.** One change shipped — a notification bell in the top bar, the affordance the product never had, composed from `buttonVariants` + `Badge` + `TopBar`'s own `actions` slot, which the platform documents as being for "notifications, theme toggle, the user menu". It is fed by `GET /notifications/unread-count`, the **only** count-shaped endpoint in the API and one whose own comment says it exists because the question "is asked wherever a badge is — every page load". A failed count renders `null`, not `0`: zero asserts "you are up to date", and claiming that because the API was unreachable is the one wrong answer available. The sharpest entry is what was **built, looked at and thrown away**: the reference's overlaid corner pill was implemented first, and reaching it meant pinning `Badge` with `absolute -end-1 -top-1 min-w-4 px-1 text-[10px] leading-4` — five arbitrary utilities that are precisely the hardcoding the phase forbids. Zoomed 6×, it rendered as a tall pink rectangle floating beside the bell rather than a pill on it. The platform has **no overlay-count primitive** at all, so the count moved inline with zero sizing overrides and the gap was written up instead. Phase 7.1's `SidebarNav` heading-contrast defect (**2.78:1**) is re-raised, still open, and now blocks the reference's visible section headings outright. A new defect is recorded and deliberately **not** fixed: the account chip renders raw UUIDs because `/auth/me` returns no display name. What is not claimed — the running application was never driven in Chromium, the six-width sweep was not done, and the dashboard, library, search, record and upload screens are **unchanged** |
| [Phase 7.4C — Arabic Pluralization Completion](./reports/phase-7.4c-arabic-pluralization-completion.md) | **COMPLETE** — 23/23, zero review markers, and the blocker that held two phases resolved by the approved policy: **the `two` form drops the digit**, because the Arabic dual already means two and printing `2 صفان` says it twice. That makes it the one form in the catalogue that deliberately ignores an interpolation variable, and it is now enforced across every plural key rather than trusted — no digit at two, a digit at one, three, eleven and a hundred. One table applied 23 times: digit + plural for `zero` and `few`, digit + singular for `one` and `other`, digit + singular accusative (تمييز) for `many`. Phase 7.3's oldest finding is closed by an assertion rather than a promise: `admin.grid.rowCount` and `reports.rowCount` rendered the same noun two different ways, and a test now compares them at all twelve counts. Two terminology ties were broken by evidence found rather than assumed — the bulk results count **`وثيقة`**, not `مستند`, because `documents.title` is `الوثائق` and `bulk.bar.label` is `الوثائق المحدَّدة`, and the catalogue's split *tanwīn* orthography (`صفًا` three times, `حدثاً` once) was normalised to the majority. Arabic changes more than one word when a count changes, and four messages show it: pronouns move (`عنه` → `عنهما` → `عنهم`), verbs move (`عدّله` → `عدّلهما` → `عدّلها`), the three bulk results take **feminine** agreement (`رُفِضت`, `مُنِعت`, `أخفقت`) now that the noun they count is named, and two duals take the oblique because of where they sit in their sentence (`يحتوي على عنصرين`, `حفظ وثيقتين`). Tests went 45 → 80; the tripwire was **retargeted, not weakened** — it asserted 23 messages carried only `other`, and now asserts exactly one does and that it is `bulk.bar.selected`, invariant by decision. RTL was verified for the first time: `renderPage` hard-coded `lang="en" dir="ltr"`, so it gained a `locale`, and five Arabic surfaces × two themes now cover `one`/`two`/`few`/`many` at 1280 and 390 — inspected as images, mirrored correctly, `صفان` with no digit beside it, `11 صفًا` unclipped on a phone. The three `bulk.result.*` messages render only after a partial-failure bulk operation the E2E suite cannot produce naturally; **no state was fabricated**, and the gap is recorded. What is *not* claimed: that a native speaker has read these 23 sentences. The rule was applied carefully to attested vocabulary and every result is tested — a different and weaker claim than "reviewed" |
| [Phase 7.4B — Arabic Pluralization Policy & Completion](./reports/phase-7.4b-arabic-pluralization-completion.md) | **BLOCKED — ARABIC POLICY DECISION REQUIRED**, and the brief's §3 decided that rather than I did: search for a documented convention, cite it if it exists, stop if it does not. It does not. The search is reproducible and its extent is the evidence — **105 markdown files** across 22 architecture documents, 20 ADRs, `docs/ui/`, `docs/operations/` and every prior report, plus `ARCHITECTURE.md`, the i18n package (which has no README at all) and every comment in the Arabic catalogue, against eleven terms including `tamyīz`, `numeral agreement`, `مثنى` and `تمييز`. Not one hit outside my own earlier reports. The three i18n rules that *do* exist are about where strings come from, logical properties for RTL, and formatting dates and numbers per locale — none about grammatical agreement between a numeral and the noun it counts. So the 23 Arabic strings were not touched, per the brief's closing instruction. **Decision B is resolved**, and by real tracing rather than by reading key names: `notifications.unreadCount` counts the caller's own notifications, `dashboard.admin.unreferenced` counts storage blobs, and the three `bulk.result.*` messages count **documents** — established by finding that `BulkResultDialog` has exactly one consumer in the entire application, the document library. The sixth, `bulk.bar.selected`, is **NOUN DECISION REQUIRED** for a reason worth knowing: it lives in `ResourceList`, shared across the library and 25+ administration screens, so `selected.length` counts whatever that list holds. English absorbs "3 selected"; Arabic cannot, because an agreeing word must know the gender and number of the thing selected. **The product genuinely does not know the noun at that call site** — a design question, not a gap in digging. Phase 7.4A's own arithmetic is corrected here: its prose said "five messages" while listing six, and six is right. Two regression tests added — a tripwire asserting exactly 23 Arabic messages still answer every category from one form, which **fails on purpose** when somebody completes one, telling them to add six wording assertions rather than widen the expectation. One catch recorded: the first version of that guard passed under vitest, which strips types, and **failed `tsc`** — reporting the suite as the gate would have reported a green that was not green |
| [Phase 7.4A — Arabic Pluralization Review & Completion](./reports/phase-7.4a-arabic-pluralization-review.md) | **BLOCKED** — deliberately, and not for want of vocabulary. The review found **one uniform obstacle covering all 23 messages**: a `two` form is a digit beside a dual, and while the catalogue writes duals well — `المراجعتان`, `اللقطتين`, `بخطوتين`, with correct case selection — **not one of the seven attested duals has a digit next to it**. Whether a badge should read `2 صفان` (redundant, the dual already means two), `صفان` (leaving `{count}` unused), or `2 صف` is a house convention nobody has set, and guessing it once would guess it 23 times. The lexical half is already settled and the report says so, so a reviewer need not re-derive it: every counted noun's plural is attested (صفوف, أحداث, مستندات, ملفات, أشخاص, عناصر, أرقام, عطلات, قرارات, نتائج, وثائق, مرات), as are three zero-constructions including `لا يوجد غير مقروء`, the exact negation of one of the 23. Three findings came out of the inventory. **The catalogue uses four different numeral-agreement strategies — and two of them for the same noun**: `admin.grid.rowCount` renders `{count} صفًا` (the 11–99 accusative *tamyīz*) while `reports.rowCount` renders `{count} صف`, the same word counted the same way on two screens; so the house policy must be decided *before* message-by-message review, or the review will reproduce the inconsistency six-fold. **Five English messages never name what they count** — "{count} were refused" — which English tolerates and Arabic cannot, since the verb must agree; flagged rather than silently rewritten. And `bulk.bar.selected` may already hold the answer, having been restructured in Arabic to `المحدَّد: {count}`, a label with a value that needs no agreement at all — offered as an alternative because the repository supports it, not applied, because reading one string as a policy is the inference this phase refuses to make. The Arabic category boundaries are VERIFIED against the runtime and pinned by 20 new tests, including the two that catch people out: **103 is `few`**, not `other`, and 100–102 are `other` while 111 is `many`. One correction of my own: my first dual search used `\b`, which does not work at an Arabic word boundary, and returned nothing — I nearly reported that the product cannot write duals at all. Checking the pattern against a known positive caught it, and the corrected search turned a wrong sweeping claim into the narrow, useful one above. **No Arabic string was added, removed or altered** |
| [Phase 7.4 — Internationalization Pluralization Architecture](./reports/phase-7.4-i18n-pluralization.md) | **PARTIALLY COMPLETE**, and the split is the point: the mechanism is VERIFIED, the English copy is VERIFIED, and **all 23 Arabic messages are ARABIC REVIEW REQUIRED** — no Arabic word was invented to close the gap. The catalogue had 28 counted strings and three ways of dodging what a number does to the words around it, two of them for the same idea in the same file: `'{count} rows'` beside `'{count} row(s)'`, and `'{count} person/people hold this role'`. The obstacle Phase 7.3 named was real — `LeafPaths` walks until it finds a `string`, so a plural object would mint `admin.grid.rowCount.one` as a `MessageKey` and move the key type under every call site in the product. The way through is a **branded** plural message: `LeafPaths` stops at it and yields nothing, a mirror `PluralPaths` yields only those keys, and because the two unions are disjoint by construction `translate` could be **overloaded** rather than duplicated — so a caller writes `t('key', { count })` with no branching, a plural message **cannot** be reached without a count, and a plain one cannot be given one. Both are compile errors rather than conventions, which is how the migration found its own consumers: **31 typecheck failures**, 28 of them fixed by typing `useTranslate`/`getTranslator` as `Translator` with no call site touched, and **3 real defects** — call sites passing `count: String(…)`, which would have worked by accident. Selection is `Intl.PluralRules` with no plural table written here and no dependency added, so adding a language means adding a catalogue. Arabic keeps the single form it already shipped, in `other`, so **Arabic output is byte-identical to before** — no regression, no false improvement, and 23 grep-able review markers where there were none. Two of my own tests were wrong and were corrected rather than the code bent to them: `Intl.PluralRules('en').select(-1)` is `one`, not `other`, and my catalogue guard descended into plural messages and reported migrated keys as unmigrated. The sharpest finding is about the harness: after the migration the visual suite reported **83 passed with no baseline change** — while the baseline visibly read "1 rows". `PIXEL_TOLERANCE` is 120 and a dropped "s" is about 66, so the screenshot could not see the fix and could not have seen the defect. The tolerance was **not** lowered; verification moved to a rendering test that asserts the real library screen says "1 row" |
| [Phase 7.3 — Product-Wide Visual Refinement & UI Consistency](./reports/phase-7.3-visual-refinement.md) | **PARTIALLY COMPLETE**, with the boundary drawn deliberately rather than by exhaustion — and three of its findings are corrections of earlier claims. Phase 7.2 left twelve hand-rolled headings; this phase located all of them and found that **two were not section headings at all but hand-rolled page headers**, one of which rendered its `<h1>` at `text-lg` — eighteen pixels, on a page whose own section headings were the same size or larger, with no way back to the document except the browser button. Ten sections migrated to `Panel` across permissions, reports, audit, notifications and templates; both page headers replaced by the primitives that already drew them. The phase's best argument for itself is what the **one new baseline** caught: five `Select`s rendering full-width because `fieldBase` is `w-full`, so choosing a role and a permission meant four stacked 1180px dropdowns — a defect three phases of source reading had walked past, found within minutes of adding an image. **Phase 7.2's library-toolbar finding is corrected**: "Columns" is not wrapping at 1280, it is `DataGrid`'s own toolbar row and is on its own line at every width; the route to one row (`toolbarActions`) was verified by reading the compiled component, and then *not taken*, because `ResourceList` backs 26 screens and the move trades `Toolbar`'s labelled group for the grid's unlabelled `div`. **Pluralisation is BLOCKED** under the brief's own stop condition, with a complete hand-off rather than a shrug: 28 `{count}` strings, two competing hedges already in the catalogue (`'{count} rows'` beside `'{count} row(s)'`, and `'{count} person/people'`), a type-system obstacle in `LeafPaths` that makes any plural entry change `MessageKey` for every call site in the product, and 168 Arabic plural forms across six categories that need a native reviewer rather than a confident guess. Both platform gaps were **re-verified against the installed package** instead of restated on trust. What is *not* claimed is as carefully recorded as what is: six of the seven changed screens have no rendered image, no axe test and no running-app responsive test, so they are consistent in source and only one of them has proof |
| [Phase 7.2 — Visual Design & Interaction Polish](./reports/phase-7.2-visual-design-polish.md) | The audit found one defect that explained most of the rest: **the application had no section language.** On the record page — the screen this product exists for — five sections carried five different hand-rolled heading treatments (`text-lg font-semibold`, `text-lg font-medium` twice, `text-sm font-semibold`, `text-sm font-medium`), two sizes, two weights, no rule, no icon, and **not one of them a landmark**. The design system had shipped the answer before this product existed — `Panel`, a bordered region with a header, a rule and a `role="region"` labelled by its own title — and it had **two uses in the whole application**; `Surface` and `Stepper` had none. So the phase is mostly deletion: nine hand-rolled section headers replaced by the component that already existed, two levels of card nesting removed (the approval instance was a card in a card in a grid), two `Card`-wrapped empty states unwrapped, nine section actions dropped to `sm` so `Download` is the one full-weight button on the record, and nine semantic icons added — the first use of `@munaxa/icons` in the workspace beyond an overflow trigger. Once borders draw the structure the space can shrink, so `gap-6` became `gap-4`. The signing ceremony gained the thing it never had — a `Stepper` saying Review → Confirm → Signed, with `error` deliberately not moving it, because a refused attempt leaves the signer where they were. Two platform components were **refused** rather than misused: `Stepper` cannot express a rejected or cancelled workflow stage and would have drawn a rejected stage as complete, which on a compliance product is the screen asserting an approval that did not happen; and `Panel`'s body padding is hard-coded, so the preview is inset by sixteen pixels it does not want — both written up as PLATFORM ENHANCEMENTs rather than worked around, and a code comment that wrongly claimed `padding={0}` fixed the second was corrected rather than left in the tree. The dashboard and the library grid were **left alone on purpose**: both already compose the system well, and §22 says a visual phase is not a licence to rebuild working screens. "1 rows" is named as a real defect and **not** fixed — `@edms/i18n` has no pluralisation at all, and patching English would leave Arabic wrong in five of its six plural categories. New coverage closes two gaps the phase exposed: a `document-record-sections` baseline, because no image had ever rendered the record page's sections *together* — precisely where the defect lived — and the record page's first-ever accessibility tests. 79 visual and responsive tests in real Chromium, 15 axe tests, 27 E2E in the running application at six widths, 15 baselines updated and every one opened and inspected in both themes |
| [Phase 7.1C — Server-Render Rate-Limit Handling & Record-Page Verification](./reports/phase-7.1c-server-render-rate-limit.md) | **COMPLETE** — and the sentence three phases could not write is now true: the record page is verified in the *running* application at 1440/1280/1024/768/430/390. Nothing about the rate limiter changed. No limit raised, no route exempted, no bypass, no weakened assertion; the suite fits inside the existing 300-per-minute budget because the record page stopped asking for things nobody had asked to see. The investigation came first and produced the whole decision: of the fifteen API requests one page view cost, **seven were for two dialogues that are closed** — the tenant's categories, confidentiality levels, users, departments, metadata fields, document types and the move picker's folders, all read straight off the source as used only inside `{editing && …}` and `{moving && …}`. A reader who opened a controlled document to read it paid for a form they never saw, on every document, every time. Those seven now load when the dialogue opens, through a server action, in the shape `loadShippedTemplate` already established on the notification templates screen — await, then open, so there is no half-populated form and no loading state to design. **15 requests became 8, and the suite's peak counter went 305 → 219 against a limit of 300**, measured the same way the cause was: an observe-only poller reading Redis every two seconds. The second half is the product defect the finding exposed. A 429 is retryable, self-healing and already rendered as "wait a moment" inside the signing ceremony — but the route error boundary receives only a digest, by design, so it *cannot* tell a rate limit from a crash and says "Something went wrong. The problem has been recorded", which is untrue twice. The page now catches `RATE_LIMITED` before it escapes the server component and renders `RateLimited` — the same `Page` + `ErrorState` shape `AdminForbidden` uses, with the API's own sentence and a manual retry, never an automatic one, because retrying on the reader's behalf spends more of a budget that is already spent. Everything else still throws, and a test pins that. Four options were rejected with reasons: a record-page route rule (it would make server-rendered pages privileged), caching the tenant catalogues (a cross-tenant leak in a database-per-tenant product), raising the limit, and splitting the suite across invented identities. Every rate-limit property was re-proved by running its own test rather than reading the code — 9/9 integration including both Redis-failure policies, plus `document.sign` at unit, integration and E2E level. 27 visual baselines re-rendered byte-identical, which is the check that this is data-plumbing and not a redesign. The record-page E2E went from a 30-second timeout into an error boundary to **3.3 seconds**, the web server log for the whole run carries no exception at all, and the test that replaced it asserts more, not less: title and status read from the database, the primary action visible, and the overflow menu **clicked open** at 390px. `Retry-After` turns out to be documented in three places and on the wire in none — recorded, not quietly fixed |
| [Phase 7.1B — Record Page Server-Render Failure Diagnosis](./reports/phase-7.1b-record-page-render-diagnosis.md) | **CAUSE PROVEN, and nothing fixed** — because the brief said to stop at the proof and it was right to. Phase 7.1A left the record page rendering the route error boundary with a correlation id and no explanation. That id, `2626124548`, was never in the API log and never could have been: it is a Next.js **digest**, a hash of the error message, which is also why it was byte-identical across four runs and four different documents — one deterministic exception, not a race. The web server's log had the whole thing: `code: 'RATE_LIMITED'`, thrown inside `documents/[documentId]/page.js` — the **server component** — carrying the API's own localised 429 detail, so it could not have originated in the web tier. Which rule, and by how much, was then **measured rather than inferred**: an observe-only poller read the counters every two seconds through a full run and caught `rl:default:…:identity:…` peaking at **305 against a limit of 300**, pinning there for exactly the length of the 30-second timeout and resetting to 11 when the window rolled. The last increment before the refusal was **+15 in one interval** — precisely the number of API calls one record-page render makes, counted independently from the page source. So the record page is not fragile: it is the most request-expensive page in the product, it runs last, and 27 tests sharing one identity in 92 seconds empty a 300-per-minute bucket before it gets there. Classified **harness**, with the auth/session thread Phase 7.1A was chasing **disproven** — a later run had no 401s at all and failed identically. Two things are raised and deliberately left unanswered: a 429 is retryable, self-healing and already rendered as "wait a moment" inside the signing ceremony, yet the same 429 during a _server_ render becomes "Something went wrong. The problem has been recorded." — unhelpful and untrue; and a fifteen-request page view puts a real user ~20 document opens from the same wall. No product code, no timeout, no sleep, no retry, no mock, no skip, no weakened assertion, no rate-limit change. One deviation is recorded rather than glossed: the local dev Redis was cleared with `flushall` where a pattern-scoped delete would have satisfied the brief literally. The record page remains **statically verified but not verified in the running application** |
| [Phase 7.1A — Record-Page Real-Browser Responsive Verification](./reports/phase-7.1a-record-responsive-verification.md) | **UNRESOLVED, substantially narrowed** — and stopped deliberately rather than closed with a manufactured fix. One evidence gap: the record page times out in the full E2E run while the library beside it passes at all six widths. The failing assertion was wrapped to dump the browser's real state, and the answer killed every prior theory at once: the page is **the route error boundary** — "Something went wrong", a correlation id, no headings at all, no skeleton, `busy: 0`. Not loading, not permission-denied, not a slow signature panel, not a locator problem. The server render threw. Four controlled experiments then all **passed** — the record page in isolation, on a second navigation, after the library, and with twenty-five browser contexts left open to recreate what a full run leaks (the suite calls `pageFor` 28 times and closes only pages, never contexts) — so six hypotheses are now eliminated and the failure needs something only a full run supplies. The surviving thread is that two *other* tests in the same run returned **401 where they expected 403 and 429**, and the suite's own sign-ins repeatedly tripped the product's `auth.login` rate limiter, costing several runs at setup. That raises a question worth a future phase — does a 401 during a server render surface as "Something went wrong" rather than sending the reader back to sign in? — recorded as a question rather than asserted, because nothing yet links the 401s to *that* render. No product code changed, no timeout raised, no sleep, no retry, no mock, no weakened assertion: the test still fails and should. Server logs now persist to disk so the next run produces the one line that decides it |
| [Phase 7.1 — UI Visual Completion: Forms, States & Responsive Design](./reports/phase-7.1-ui-visual-completion.md) | The phase that measured first. Phase 7 ended with a list of what it had not done; this one checked that list against four screens at six widths in Chromium before changing a line, and the measurement rewrote it. **Two of Phase 7's own claims were wrong.** Its platform gap "no table-shaped skeleton" is withdrawn — `DataGrid` ships `SkeletonRows` and `ResourceList` has passed `loading` since Phase 2, so every list in the product has had one all along. And Phase 7 shipped **two WCAG failures its own suite could not see**: the rail's four section headings at **2.78:1**, because its baseline rendered an *untitled* group, and the record page's revision badge at **4.18:1**, because no baseline rendered that page at all. Adding the coverage found both within minutes, and neither ships. Four layout defects came out of the widths: the record page overflowed by **198px at 390px** because a sixty-four character content digest has no break opportunity and set the automatic minimum size of its grid item — the same digest a `truncate` had been quietly cutting to a third since Phase 3, beside a comment insisting it is shown in full; search overflowed by 24px; and **the document library rendered an empty row at 390px**, eight columns at forty pixels each, naming no documents on the screen an EDMS is most likely to be opened on away from a desk. Fixed with fewer columns at narrow widths through the platform's own `useMediaQuery` — the same `ResourceList`, the same `DataGrid`, no second grid and no fork. Plus `FormSection` (`fieldset`/`legend`, no card-in-card) on the upload dialogue, and a page-shaped route skeleton replacing a spinner on every navigation. 27 baselines, up from 18, every changed image inspected — one of those inspections is why the mobile library fix exists at all. A new permanent responsive spec covers four screens × six widths; in the running application the library is verified at all six widths and the phone drawer with it, after a diagnosis that took two wrong theories and one decisive experiment — while **the record-page E2E test still does not pass and its cause is not established**, which the report states as an open failure rather than rounding up to green |
| [Phase 7 — Visual Modernization & UI Polish](./reports/phase-7-ui-modernization.md) | **COMPLETE with three named limitations.** The audit came first and reframed the phase: this application owns almost no visual language of its own — the palette, type scale, radii and shadows live in `munaxa-platform` and arrive through one `@import`, and `ARCHITECTURE.md` forbids this repository from holding a colour at all. So Phase 7 could not be a re-skin, and every weakness it found was a *composition* weakness instead. Four of them mattered: twenty administration screens sat in a `Page` with a `PageHeader` and **no workspace screen did** — seven opened with a hand-written `<h1>` and two had no header at all, while `Breadcrumb` had **zero uses anywhere** despite the architecture calling for contextual breadcrumbs since Phase 0; thirteen document lifecycle states rendered as one identical grey badge, so a reader scanning a folder had to read every one; the record page opened with up to eight equal-weight buttons above the document's own name while the number that *identifies* a controlled record sat fourth in the second card; and the rail listed ten destinations in one undifferentiated run. All four are closed by two shared extractions and a shell change. The phase's most useful hour was spent in the browser rather than in the editor: giving `APPROVED`/`PUBLISHED` their semantically correct green **failed WCAG AA at 4.21:1 light and 3.98:1 dark**, so the green was not shipped and the palette defect is written up for the platform; a fixed title column crushed every column after it into single-letter ellipses and was measured, reverted and measured again; and a wrapper span inside `Badge` turned a documented inherited contrast defect into an unrecognised one that failed the build. Six of eighteen baselines re-recorded, each inspected as an image first, twelve deliberately untouched. Coverage went **up** — four tests added, three adapted with real clicks rather than weakened — and 24/24 E2E pass against the shipped API and production web build in real Chromium. Forms, skeletons and narrow-screen widths are stated as not done rather than claimed |
| [Post-6.13 — Production Closure & Targeted Remediation](./reports/post-6.13-production-closure.md) | **PRODUCT DEVELOPMENT GO · PRODUCTION RELEASE NO-GO**, on one condition that is not engineering work. Two passes. The first answered the PITR ownership question by reading what the repository *does not contain* — no infrastructure-as-code of any kind, and a deployment architecture naming three legitimate production targets rather than one — and stopped, because **PITR ownership is a production architecture decision and cannot safely be implemented from this repository alone**; it also closed the scheduler-regression gap with a test that asserts a job *reached its handler* rather than that the broker accepted it. The second pass closed Gate 1: the five scheduled jobs whose business effects had never been observed were driven through their real preconditions on a running deployment, and **retention disposition was finally watched doing its job** — a real soft delete under a real policy, a sweep that refused to destroy and demanded review, a records manager approving through the API, and the next sweep purging the document, writing a tombstone and leaving the hash chain intact and verified. The three digests and `release-batches` likewise, idempotent on re-fire, with the neighbouring tenant untouched. Two defects found on the way: **the entire bulk-operations API was unreachable** — `POST /documents/bulk/restore` and `GET /documents/bulk` were shadowed by `/documents/:id/...` and answered 500, since Phase 16 — fixed by one line of controller ordering and guarded by a test that asserts the ordering itself; and a **revoked delegation notifies nobody**, because the event omits the two identifiers its consumer requires, reported rather than fixed because changing a shipped event's shape is a product decision. The only substitution anywhere was the clock, and only because the product deliberately refuses to dispose of a record sooner than a person configured |
| [Phase 6.13 — Final DR, Scheduler & Production-Evidence Closure](./reports/phase-6.13-final-dr-scheduler-closure.md) | **PARTIALLY COMPLETE · NO-GO**, narrowly — and the thing four phases had been circling is finally done: **combined disaster recovery, verified end to end.** Two tenants, each with a document whose bytes went in through the presigned path Phase 6.12 repaired, backed up database *and* bucket, restored into a genuinely empty PostgreSQL cluster and a genuinely empty MinIO, and then **served by the real application** — content retrieved through the product's own audited download path and returning **byte-identical and digest-identical** to what was uploaded. That is the assertion every earlier phase stopped short of: a restored row that resolves to the right object, for the right tenant, with the right bytes. Cross-tenant refused in both directions on metadata *and* content, with identifiers that really exist in the other restored database. The chain verified on both restored tenants and signed fresh checkpoints into the restored store. Chromium confirmed it from a browser: the restored document's title rendered, the neighbour's did not. `storage-backup.mjs` drives the product's **own** S3 adapter rather than a second S3 client, because ADR-0007 §1 allows one storage implementation and Phase 6.12 proved the checksum handling that makes an object verifiable lives inside it. What still blocks GO is short and specific rather than unknown: retention disposition has never been observed acting — a compliance control in a document-control product; nothing yet fails when a job goes declared-but-inert, a class of defect that has now bitten twice; and **PITR does not exist** — no `archive_mode`, no `archive_command`, no backup tool anywhere in the repository, against a documented 5-minute RPO. That last one is a decision for a person, not another phase. No new defects found, which after five phases that each found one is itself worth recording |
| [Phase 6.12 — Cloud Storage Integrity](./reports/phase-6.12-cloud-storage-dr-and-scheduled-regression.md) | **PARTIALLY COMPLETE** — Phase 6.11's P0 fixed, in the strongest form the architecture allows, by two lines that turned out to be two halves of one omission. `UploadTargetInput.checksumSha256` had been on the storage port since Phase 3 and `StorageService` had always passed it; the S3 adapter **dropped it**, so the object was written with no checksum — and `head()` never sent `x-amz-checksum-mode: ENABLED`, so S3 withheld the checksum even from an object that had one. Signing the digest was necessary and *not sufficient*: the write had to record it and the read had to ask for it. What makes the result integrity rather than bookkeeping is where ADR-0007 leaves no choice — §6 forbids bytes passing through the API, so the product cannot hash the object itself, and §2 makes the key *be* the digest, so a wrong digest is a wrong key, a broken dedupe and a broken reference count. The only arrangement satisfying both is the store verifying the digest at write time against a value inside the signature, and that is what now happens: wrong bytes are refused `400 XAmzContentChecksumMismatch`, a client substituting its own digest is refused `403 SignatureDoesNotMatch`, and nothing is stored in either case. Two tenants uploaded real PDFs through the real presigned path; the persisted digest equals the SHA-256 of the actual bytes; the objects sit content-addressed under their own prefixes. Guarded by four integration tests against a real store and — the part that stops it recurring — **MinIO is now a service in the CI integration job**, because the defect survived eighteen phases for the simple reason that nothing in CI had ever spoken to an object store. Left undone and named rather than implied: the combined database + object-storage restore, and the five schedules still without preconditions |
| [Phase 6.11 — Scheduled Work Verification & Object-Storage Recovery](./reports/phase-6.11-scheduled-work-and-object-storage-recovery.md) | **PARTIALLY COMPLETE** — the phase that asked whether Phase 6.10's newly *reachable* schedules actually do anything, and found a second whole path that had never worked. Thirteen schedules discovered from code, all thirteen found registered in the broker with their real payloads, all thirteen replayed through producer → broker → worker → service, and **eight with their intended side effect observed in the database and the trail**: a document driven `PUBLISHED → EXPIRED` with its audit row and its outbox row, a delegation ended, an abandoned upload session settled, a blob read back and stamped `UNREADABLE` with an `INTEGRITY_MISMATCH` beside it, the chain verified and re-checkpointed, an email retried with its backoff, a webhook delivery attempted and rescheduled — refused, as it happens, by Phase 17's outbound allow-list doing its job. Five ran with nothing to act on and one has no configured sink, and each is named rather than dressed up. Then the second objective hit a **P0**: on the S3/R2 driver **no upload can ever complete.** The presigned target signs `content-type` and `content-length` and never asks the client for a checksum, so S3 stores the object without one, `head()` answers `checksumSha256: null`, and `completeUploadSession` discards the upload and returns `415` — *"Storage could not confirm the file's digest."* `LOCAL` is unaffected because it hashes the bytes itself, which is exactly why nobody noticed: the failure is invisible on a single-server install and total on every cloud one. Reproduced through the real API against real MinIO, with the empty bucket afterwards as the proof. Deliberately **not fixed**: the two candidate boundaries attest different things — the store's word versus the product's — and the adapter's own comment argues one way while convenience argues the other. Object-storage DR therefore stays NOT VERIFIED, because there was no document content to back up. What *is* proven about S3: the tenant-scoping wrapper and the checkpoint store work against a real bucket, with two tenants' signed checkpoints under two correct prefixes |
| [Phase 6.10 — Disaster Recovery Rehearsal & Notification Production Path](./reports/phase-6.10-disaster-recovery-and-notification-verification.md) | **PARTIALLY COMPLETE** — the two gaps Phase 6.9 named, closed as far as the evidence honestly allows, and the P0 the phase was not looking for. **Every scheduled background job in this product was refused by the broker.** BullMQ rejects a custom job id containing `:` unless it happens to split into exactly three parts — a compatibility carve-out for its own older repeatable ids — and every scheduled fan-out embeds the firing's own `repeat:<name>:<epoch>`. So the fan-out threw on its first line and no tenant job was ever created: **no email had ever been sent, no retention swept, no expiry fired, no chain verified, no webhook retried, no search projection written.** The outbox dispatcher was untouched because `outbox:<row>:<lane>` is three parts by coincidence, which is precisely why nothing noticed — in-app notifications arrived, so the pipeline looked alive. It surfaced because §23 asked for retry evidence and retry needs a delivery pass. Fixed at one boundary, and guarded by an integration test that asks a **real broker** to accept all twelve shapes this product derives. Beside it: the **first restore in this repository's history** — two tenants, two databases, into a freshly `initdb`'d cluster, compared table-by-table across the whole schema with **zero differences**, RLS enabled *and* forced on 77 of 77 tables, `edms_app` neither superuser nor `BYPASSRLS`, and the product then booted against it and driven through real Chromium. The audit assertion is the one that counts: the restored deployment's verification **resumed from the checkpoint the source had signed at sequence 19** and walked only the 2 events after it — which it could not have done if the restored rows no longer recomputed. Also a defect in the documented procedure itself: `pg_restore --create` ran *before* the cluster roles existed, so every ownership and grant statement failed and `pg_restore` **still exited zero** — a procedure that produced an unusable database and reported success. And the comparison that nearly reported a catastrophe of its own, because the source cluster's owner was a superuser and bypassed the row-level security the destination's correctly could not. The notification half is unambiguous: a real approval in one browser, a real notification on another person's screen, tied to its outbox row by an idempotency key that contains it, in English and Arabic, with retry observed. Object storage is **NOT VERIFIED** and the status word says so |
| [Phase 6.9 — Real-Browser Workflow Completion](./reports/phase-6.9-real-browser-workflow-verification.md) | **COMPLETE WITH FINDINGS** — the eight workflows Phase 6.8 left NOT VERIFIED, executed through real Chromium against a built API and a built web server, and the **thirteen screens** that discovery proved no user could open in any production deployment. Twelve of them — the document library and eleven administrative lists — threw *before rendering*, because a server page cannot receive an array exported from a `'use client'` module: Next hands it a client reference, and `new Set(FILTER_KEYS)` failed with `function is not iterable`. The thirteenth was `/delegations`, the third instance of the `pageSize: 200` defect Phase 6.6 fixed in two other places. Both are the same defect Phase 6.6 met as `EMPTY_FORM_STATE`, and both were invisible to 1,600 green tests for the same reason: nothing had ever loaded a built page. Fixed under §8's minimal-fix rule — 33 sort-field and filter-key constants relocated to one module that crosses no boundary — and nothing else was changed. Four screens Phase 6.8 could not judge turned out **healthy**, and §17 records why three of my own early calls on them were wrong: `body.textContent` includes the RSC payload, which embeds every error-boundary string a route could ever render, so a healthy page "contains" its own failure text. Phase 6.8's P0 is re-asserted where it would actually occur — opening a document in the browser issues **fewer than ten** content URLs against the 6,704 the defect produced. Tenant isolation, persistence and the audit trail verified through the browser rather than through the API. Left standing and named rather than fixed: the notification production path is unproven end to end, and `GET /documents/:id/revisions` answers `404` for a document with no revision |
| [Phase 6.7 — Rate Limiting Completion (Part A)](./reports/phase-6.7-rate-limiting-completion.md) | **COMPLETE.** The rule table that had sat unread since Phase 5, wired into a guard registered ahead of authentication — because credential stuffing is unauthenticated by definition — counting through the existing `CachePort`, whose Redis adapter is `INCR` plus `EXPIRE NX` in one `MULTI` and therefore atomic across connections. Proven at two levels, and the second level is the point: nine guard tests prove the mechanism (two independent adapters sharing one budget, tenant isolation, concurrency, fail-closed on Redis loss), and a booted Nest application proves a real HTTP request reaches it — six `POST /api/v1/documents/:id/signatures` calls where the first five are refused by authorization and the sixth by the limiter with `429`, a shape that can only occur if the guard runs first. Writing that last test was not a formality: it found that the route table never matched a real path, because `configureApp` mounts everything under `/api` and the matcher stripped only the version — **every rule would have been inert in production while all nine guard tests reported green**. The third time this sequence has found a control that was declared, configured and unreachable, after Phase 6.3's phantom permissions and Phase 6.4's unread `DeliveryState.FAILED`. Both Phase 6.6 blockers are now closed |

### Phase 6.2 — bulk operations and asynchronous processing

> The second phase built from the Phase 6.0 roadmap, and the one that closes its P0.

| Document | Purpose |
| --- | --- |
| [Phase 6.2 — Bulk Operations & Async Processing](./reports/phase-6.2-bulk-operations-async.md) | A tenant setting that had been read by nothing since Phase 16, so a request naming up to five thousand objects executed every one of them inside one HTTP request — the failure the setting's own documentation describes. Why three of the six findings in the phase's own commissioning audit did not survive inspection: `maxObjects` had always been enforced, `RUNNING` had always been written, and the item table had always been an upsert keyed for redelivery. The obstacle nobody had seen: a `BulkPlan` carries closures, so the lane had no producer not because the consumer was unwritten but because there was nothing to tell one what to do — and why the fix is a payload column that is deliberately *not* the audited one, since `parameters` is copied into the audit row and 13 §3 requires it minimised. Why an `APPLIED` item row now commits inside its object's transaction while the other three outcomes keep Phase 16's placement: the two cases have different requirements, and the gap between them is a second document on an `UPLOAD` redelivery. Why the requester's authority is re-read when the job runs rather than copied when it was asked for, and why that is a second port rather than a reuse of Reporting's. The two architecture violations the repository's own lint caught in this work — core importing a module, and a test reaching into another module's internals — and how each was fixed where the rule pointed rather than worked around. A Phase 6.1 test corrected rather than deleted, with the cause of its flake finally understood. And one new finding raised on the way past: `context.roles` holds role keys where the ACL subject expects identifiers |

### Phase 6.1 — document lifecycle completion

> The first phase built from the Phase 6.0 roadmap. It closes four of that audit's rows and opens
> three new ones.

| Document | Purpose |
| --- | --- |
| [Phase 6.1 — Document Lifecycle Completion](./reports/phase-6.1-document-lifecycle-completion.md) | Two states the design table allowed from Phase 0 and nothing performed, finally performed: `ARCHIVED`, which until now happened only as a retention sweep's side effect, and `EXPIRED`, which was reachable by nothing at all — so a document could carry an expiry date that never arrived. Why `LEGAL_TRANSITIONS` was not edited by one line, and why that is the evidence the lifecycle was completed rather than redesigned. How the audit action varies without a second lifecycle: two optional fields on the one transition method, omitted by every caller that predates the phase. Why the two archive paths stay two code paths and converge on one invariant — `setStatus` matches on `deleted_at IS NULL` and the retention disposition's job includes archiving deleted records, so merging them would have meant weakening the predicate that keeps any caller from moving a deleted document's status. Why the expiry sweep is hourly rather than nightly (one firing is after midnight for some tenants and hours before it for others), why it shares the `retention.run` lane rather than taking one (a second subscriber would race the lane's existing consumer — the defect that gave delegation a lane of its own), and why `effective_to` is inclusive. The permission that was in the catalogue, in the matrix, seeded to two roles and named by no route for eighteen phases, and why that is worse than an absent one. The defect this phase introduced and its own integration suite caught before it shipped — a stated reason in the payload rather than in the trail's attested column, which would have left an archive's justification outside Phase 9's hash digest. Why `LINKED` was deliberately **not** written. And every gate run against a real PostgreSQL with two tenant databases: 594 integration tests, zero skipped |

### Phase 6.0 — enterprise feature completion audit

> The audit that turns nineteen phases of delivery into a roadmap. Numbered by the brief that
> commissioned it; it follows Phase 5.2 and opens the feature track that Phase 6.1 continues.

| Document | Purpose |
| --- | --- |
| [Phase 6.0 — Enterprise Feature Completion Audit](./reports/phase-6.0-enterprise-feature-completion-audit.md) | Every module measured against its own architecture document, ADR, permission matrix, audit catalogue and notification catalogue — by reading the code rather than the reports about it. The four things genuinely missing rather than merely unpolished: a lifecycle that stops short of its own state machine, so `ARCHIVED` happens only as a retention sweep's side effect, nothing watches `effective_to`, and `EXPIRED` is unreachable; a bulk path whose asynchronous half was never wired, so `bulk.synchronousLimit` is a setting nothing reads, `documents.bulk` is a lane with no producer and no consumer, and five thousand transactions run inside one HTTP request — the exact failure the setting's own documentation describes; four shipped API surfaces with no UI at all, two of which have their server actions already written and called by nothing; and sharing, watching, acknowledgement and document linking, three of which the notification catalogue already names as recipient classes. The mechanical census: zero `TODO`s, exact `en`/`ar` key parity at 1 383, and exactly three permissions that are documented, seeded to roles and enforced by nothing. Why Phase 19's nine-row platform violation list was re-verified rather than carried forward, and which seven of the nine are closed. A prioritised roadmap in which twenty-four of twenty-seven items are a screen, a consumer, a writer or an enforcement against something already built — and none is a rebuild |

### Phase 5.1 — UI foundation completion and platform design system adoption

> Numbered by the brief that commissioned it rather than by position: it follows Phase 19 and
> finishes what that audit found, on the UI-foundation track rather than the feature track.

| Document | Purpose |
| --- | --- |
| [Phase 5.1 — UI Foundation Completion](./reports/phase-5.1-ui-foundation-completion.md) | The ten deliverables, and the three things they turned up. Why the regression guard took two attempts — the first draft passed against a deliberately broken build, because `css.includes('.bg-primary')` matches inside `.bg-primary-strong`, so every sentinel resolved on the strength of a different class and the check reported a healthy stylesheet missing 59% of itself; a guard that fails open converts an absent check into a false assurance, which is why its helpers now have tests of their own and why the sentinel list is documented as the weaker signal. The second silent defect, of Phase 19's exact family: `--color-surface-hover` referenced by five hover states and defined nowhere in the platform, the tokens or this repository, so five affordances had never done anything and nothing could have caught it. And a correction to Phase 19 itself, which called `SkipLink` unused and its absence an accessibility defect — it is used, through `AppShell`'s `skipLinkLabel`, and a symbol grep cannot see a prop. What moved: every raw table to the platform's `Table` and the `scope="col"` the hand-written markup omitted, both audit trails to `Timeline`, twelve hand-styled form controls to `Field`/`Input`/`Select`, adoption 54 to 62 of 180. What deliberately did not, with the type signatures the platform would need instead: `FileManager`, whose fixed name/size/modified columns cannot carry a document number or a confidentiality mark; and `ApprovalFlow`, whose binary all/any cannot express `QUORUM` or `PERCENT` and whose statuses have no `CANCELLED` — mapping which onto `skipped` would tell an auditor a control was evaluated and did not apply when it was never reached, which is a false statement in a compliance record rather than a visual imperfection |

### Phase 19 — shared platform compliance and integration audit

| Document | Purpose |
| --- | --- |
| [Phase 19 — Shared Platform Compliance & Integration Audit](./reports/phase-19-shared-platform-compliance.md) | The audit that had to install the platform before it could say anything true, and what reading it rather than assuming it produced. The critical finding: a Tailwind `@source` pointing at a transitive dependency pnpm does not link, which removed 64% of the design system's stylesheet — 136 of 249 platform utility classes generating no CSS rule at all, every one of them a class the product never writes itself, `bg-card` and `bg-primary` and every surface and brand colour the platform's own components declare — through five green gates and a whole production-readiness phase, because a stylesheet that is silently too small is not a type error, a lint error, a test failure or a build failure. One line fixes it and the report measures the fix. The second finding needed the same install: the platform exports 180 components and this product uses 54, and among the other 126 are a `Table`, a `Timeline`, an `ApprovalFlow` and a `FileManager` that Munaxa Docs each built by hand, against a rule the development recommendations put first in their table. Why the brief's sections on authentication, audit, logging and notifications could not be followed — the registry publishes seven `@munaxa/*` packages and every one is design system or lint configuration — and why the local implementations of those are the product rather than violations of it. Why both real findings have one root that is not carelessness, why the answer to *should we start the UI* is no, and why the honest description is that the UI has never actually been seen |

### Phase 18 — production readiness

| Document | Purpose |
| --- | --- |
| [Phase 18 — Production Readiness](./reports/phase-18-production-readiness.md) | Sixteen brief items that are mostly seams which already existed, and finding out which was the phase's first work. Why binding `METRICS` is a question about *shape* rather than about whether, why the exporter pulls, and why the label rule became a catalogue the exporter enforces; why tracing gets a different answer from metrics; what a production image contains, answered with the commands that show the registry is reachable and the private scope's credential is not; how Phase 15's evidence-CSV objection was answered rather than ignored; why Phase 14's "key management service" row was about rotation rather than about an integration; how Phase 12's SMTP refusal was answered by producing the evidence it asked for; the integrity sweep three reports called this phase's; why a security suite that restates a tested property is ceremony; why the load harness exists and its numbers do not; what the phase costs, and what it deliberately does not do |

### Phase 17 — API and integration

| Document | Purpose |
| --- | --- |
| [Phase 17 — API & Integration](./reports/phase-17-api-integration.md) | Eleven brief items that are four groups, and sorting them was the first decision: SSO, Azure AD and Google Workspace are one OIDC adapter; LDAP and Microsoft 365 are two different things and neither is federation; migration and import APIs are mostly Phase 16's. Why a machine caller is a delegated subject and what the alternative would have cost; why a webhook is not a notification; why the outbox routing table's default changed; what `jose`, `ldapjs`, `samlify` and `cbor` actually are, answered with commands, and why hand-writing OIDC verification is not the trade SAML refuses; how 17 §6's SSRF row stopped being unfalsifiable; why the OpenAPI document and the OpenAPI explorer are two things; what the phase costs, and what it deliberately does not do |

### Phase 16 — advanced document features

| Document | Purpose |
| --- | --- |
| [Phase 16 — Advanced Features](./reports/phase-16-advanced-features.md) | Twelve brief items that are three groups, and sorting them was the first decision: five capabilities already built, two with no design at all, and five bulk operations over machinery built in anticipation of them. Why every bulk operation is a permission decision repeated N times and what the shortcut would have cost; why a bulk route cannot carry `@ScopedTo` and what replaced it; why one transaction per object is the difference between partial success and a silent skip; the four readings of "digital signature" and why this product means a witnessed Part 11 attestation and may never call it qualified; why a bulk operation writes N + 1 audit rows and never an identifier list; why bulk export diverges from two existing export mechanisms and produces a manifest rather than an archive; which operations notify and to whom; what `sharp` and `@pdf-lib/fontkit` actually are, answered with commands; how 19 §5's fairness claim was made true; what the phase costs, and what it deliberately does not do |

### Phase 15 — enterprise reporting

| Document | Purpose |
| --- | --- |
| [Phase 15 — Enterprise Reporting](./reports/phase-15-reporting.md) | The last Phase 0.5 module whose contracts shipped and were never implemented, bound at last — and bound so it cannot become the door around four phases of narrowing. Why a report never widens the audience of the surface it summarises, and why that made `document:restore`, `retention:manage` and `audit:view` conditions rather than alternatives; why the rows are scoped by the *module that owns the table* rather than by a query in this one, and what materialised read models and the search index would each have cost; the Excel decision and the three answers not taken; why the export runs under the requester's reach and reads their roles at the instant it runs; why "scheduling ready" ships the seam and names the phase that closes it; what a report cell may never translate; the two defects the tests found; what the phase costs, and what it deliberately does not do |

### Phase 14 — enterprise security

| Document | Purpose |
| --- | --- |
| [Phase 14 — Enterprise Security](./reports/phase-14-security.md) | The ACL phase: the entries ADR-0005 described in Phase 0 and nothing had, the walk over the materialised paths, deny precedence, and `@ScopedTo` on the object routes — so `AclGuard` fires for the first time and Phase 9's `ACCESS_DENIED` is finally reached. The resolver **extended, not rebuilt**: four method signatures unchanged and not one caller touched. Why an inheritance break truncates the chain for *both* effects rather than only for grants; how a relational list gets a predicate the search index's token arrays cannot give it, and why putting it in the repository is what made Phase 13's inherited-counts claim true; what an ACL change costs the search index and what the index serves in the meantime; why MFA is TOTP and not WebAuthn, and why its policy half went to 17; why Share is declined and who owns each reading of it — and the defect six phases old that only firing the guard could find |

### Phase 13 — dashboard

| Document | Purpose |
| --- | --- |
| [Phase 13 — Dashboard](./reports/phase-13-dashboard.md) | The last Phase 0.5 module whose contracts shipped and were never implemented, bound at last — and bound so that its own rule cannot be broken: the module has no `infrastructure/` and no Prisma import, so a widget has nothing to count rows with, and every figure is built from the predicate the list or the inbox it summarises is built from. Why a count is a disclosure and why a refused tile is *absent* rather than zero; the decisions the brief left open — where "Checked Out" comes from, whether the activity feed gains a tenant-wide reader, where the administrator dashboard lives, where Phase 15 begins, and why storage reports bytes but never a quota; why nothing is cached on the busiest route in the product; what the phase costs, and what it deliberately does not do |

### Phase 12 — notifications

| Document | Purpose |
| --- | --- |
| [Phase 12 — Notifications](./reports/phase-12-notifications.md) | The framework Phase 1 built with no producers, finally called: the `notifications.deliver` lane's first consumer, fourteen rows of 18 §4 given catalogue entries and templates, the four phases that deferred delivery here discharged — and the outbox routing table found to have been silently discarding every `delegation.*` event since Phase 11. The decisions 18 left open — what a held message *is*, where a digest's list is composed, what holds a suppression, and which mail adapter is worth building — why the recipient walk is the phase's central safety property, what the phase costs, and what it deliberately does not do |

### Phase 11 — delegation

| Document | Purpose |
| --- | --- |
| [Phase 11 — Delegation](./reports/phase-11-delegation.md) | The most carefully pre-cut seam in the product, bound at last: `DELEGATION_REPOSITORY` and `DELEGATION_SERVICE` given a table, the one check Phase 4 named relaxed in the one place it lives, a task that never moves so a revocation has nothing to reassign, authority read at the instant of the decision rather than copied at creation, a chain bounded by arithmetic and a cycle refused before any setting is consulted, an emergency delegation whose bypass is recorded in the trail's own attested column, expiry as a predicate with a lane that only records it — the decisions 07 §4 left open, why 08 §3 lost a clause rather than the resolver gaining a subject, what the phase costs, and what it deliberately does not do |

### Phase 10 — soft delete & retention

| Document | Purpose |
| --- | --- |
| [Phase 10 — Soft Delete & Retention](./reports/phase-10-soft-delete-and-retention.md) | The deletion half made real: one table answering what a delete cascades to after three modules had answered it locally, the tombstone that keeps a purged document's number where the purge cannot reach it, the recycle bin 16 §2 named in Phase 0, the `retention.run` lane consumed at last with both its schedules, the legal hold finally throwing the error code that had waited since Phase 0.5 — the decisions the specification left open, why Phase 9's partitioning trigger fired and the answer was still no, what the phase costs, and what it deliberately does not do |

### Phase 9 — audit & compliance

| Document | Purpose |
| --- | --- |
| [Phase 9 — Audit & Compliance](./reports/phase-9-audit-compliance.md) | The audit architecture's read half made real: the digest widened and versioned rather than backdated, the timeline filtered at its subject through the resolver Phase 8 bound, signed checkpoints kept outside the database they attest, the daily verification finally firing on a named schedule, read auditing buffered as §5 always claimed, the streamed evidence bundle and its honest manifest, `ACCESS_DENIED` given the writer it never had, and the event catalogue reconciled with the code — what the phase costs, and what it deliberately does not do |

### Phase 8 — search

| Document | Purpose |
| --- | --- |
| [Phase 8 — Search](./reports/phase-8-search.md) | The search architecture made real: the `search.index` lane finally consumed, the weighted index with its materialised ACL subjects, the permission predicate inside the query, Arabic beside English in the query path, the resumable shadow-table rebuild, saved and recent searches — the decision that finally bound `ACL_RESOLVER` and why search is what forced it, what the phase costs, and what it deliberately does not do |

### Phase 7 — document preview

| Document | Purpose |
| --- | --- |
| [Phase 7 — Document Preview](./reports/phase-7-document-preview.md) | The preview architecture made real: the renderer registry bound with four plugins, the lanes finally consumed behind the antivirus gate, watermark and print made meaningful, the compare API's text state filled in, the decisions 14 left open and how each was taken — the DWG refusal, the client-side raster, the per-user mark — and what the phase deliberately does not do |

### Phase 6 — revision control

| Document | Purpose |
| --- | --- |
| [Phase 6 — Revision Control](./reports/phase-6-revision-control.md) | The revision architecture made real: the check-out lock under concurrency, publication superseding in one transaction, restore as a row rather than a copy, the decisions 06 and 10 left open and how each was taken, the numbering seam's revision-cycle branch, and what the phase deliberately does not do |

### Phase 5 — document numbering

| Document | Purpose |
| --- | --- |
| [Phase 5 — Document Numbering](./reports/phase-5-document-numbering.md) | The issuance half of the numbering architecture: the counter under concurrency, the decisions §2 and §3 left open and how each was taken, what binding Phase 4's seam proved, and what the phase deliberately does not do |

### Phase 4 — the workflow engine

| Document | Purpose |
| --- | --- |
| [Phase 4 — Workflow Engine](./reports/phase-4-workflow-engine.md) | The approval engine and the async half of the architecture: what one primitive bought, why the calendar was built rather than deferred, the concurrency defect the integration suite found, and what the phase deliberately does not do |

### Phase 3 — the document library

| Document | Purpose |
| --- | --- |
| [Phase 3 — Document Library](./reports/phase-3-document-library.md) | The first phase to store a customer's bytes: the two storage adapters, the upload pipeline and its gate, what it costs, and what it deliberately does not do |

### Phase 2.5 — the deployment-agnostic foundation

| Document | Purpose |
| --- | --- |
| [Phase 2.5 — Per-tenant infrastructure](./reports/phase-2.5-per-tenant-infrastructure.md) | The refactor that gave every tenant its own database, storage and index — what moved, what did not, and what it costs |

### Phase 2 — administration

| Document | Purpose |
| --- | --- |
| [Phase 2 — Administration](./reports/phase-2-administration.md) | What the Administration phase built, the decisions worth carrying forward, and every limit it left deliberately in place |

### Phase 0.5 — the technical skeleton

| Document | Purpose |
| --- | --- |
| [Architecture compliance report](./reports/phase-0.5-architecture-compliance-report.md) | The formal gate before Phase 1: what was built, what holds, what is owed, and the verdict |
| [Architecture gate verification](./reports/phase-0.5-architecture-gate-verification.md) | Independent re-audit of that report — every falsifiable claim re-derived from the repository |
| [Technical debt](./reports/phase-0.5-technical-debt.md) | Every boundary the skeleton leaves owing, with an owner and a trigger |

### Phase 0 — the architecture

| Document | Purpose |
| --- | --- |
| [Repository analysis](./reports/repository-analysis.md) | What exists in this repository, what Munaxa Docs reuses, what it must not duplicate |
| [Technical debt report](./reports/technical-debt.md) | Defects and drift found during the Phase 0 analysis, with owners |
| [Risk assessment](./reports/risk-assessment.md) | What can go wrong, likelihood, impact, mitigation |
| [Development recommendations](./reports/development-recommendations.md) | How to build phases 0.5 → 18 without repeating known mistakes |

## Maintaining this index

Adding a document means adding a row here in the same commit, and a row in the repository index
this index if it matters outside a single document. A document not linked
from an index is invisible.
